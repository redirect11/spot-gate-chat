{
  "name": "spot-gate-chat-functions",
  "description": "Claude-backed chat bot engine (moderation + persona)",
  "private": true,
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "bots:deploy": "node scripts/deploy-bots.mjs",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
